export type Currency = {
    id: number,
    isoCode: string,
    description: string,
    enabled: boolean,
    createdAt: string,
    createdBy: string,
    updatedAt: string,
    updatedBy: string
}