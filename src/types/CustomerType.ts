export type CustomerType = {
    id: number,
    name: string,
    description: string,
    enabled: boolean,
    createdBy: string,
    createdAt: string,
    updatedBy: string
    updatedAt: string,
}